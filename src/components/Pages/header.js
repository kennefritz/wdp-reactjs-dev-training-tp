import React from 'react';
import { Layout } from 'antd';
const {
    Header,
} = Layout;

export default () => (
    <Header
        style={{
            background: 'rgb(50, 96, 96) none repeat scroll 0% 0%',
            color: '#fff',
        }}
    >Header</Header>
)